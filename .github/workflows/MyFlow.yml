
on:
  push:
    branches:
        - "main"jobs:
    lab-job:
        run-ons: ubuntu-latest
        steps:
            - name: Start job
              run: echo 'Hello DevOps' 
 lab-job:
        run-ons: Run1
        steps:
        - name: Run job
        run: echo 'run Job'
